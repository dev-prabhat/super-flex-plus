import { Header } from "../components/Header"
import { NavBar } from "../components/NavBar"
import "./home.css"

export const Home = () => {
    return(
        <main>
          <Header/>
          <NavBar/>
        </main>
    )
}